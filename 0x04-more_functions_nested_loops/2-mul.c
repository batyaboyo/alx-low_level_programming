#include "main.h"

/**
 * mul - Entry point
 *
 * @a: a is a variable name
 *
 * @b: b is a variable name
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
int mult;

mult = (a * b);
return (mult);
}
